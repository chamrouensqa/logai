POSTGRES_DB=logai
POSTGRES_USER=logai
POSTGRES_PASSWORD=change_this_postgres_password

# Set to your Ubuntu server IP or DNS name for browser-side direct upload URL.
# Example IP: 192.168.1.50
# Example DNS: logai.local
SERVER_HOST_IP=127.0.0.1
